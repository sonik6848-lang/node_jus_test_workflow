jobs:
    my_job:
        runs-on: ubuntu-latest

        steps:

        - name: checkout code
          uses: actions/checkout@v3

        - name: Build and test
          run: |
              npm install
              npm test

        - name: Notify
          if: always()
          run: |
              npm run notify   